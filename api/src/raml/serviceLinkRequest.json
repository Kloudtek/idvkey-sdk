{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "userRef": {
      "type": "string",
      "description": "User ref to assign to service link"
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "URL on service website where the user will be redirected to after accepting the link request"
    },
    "cancelUrl": {
      "type": "string",
      "format": "uri",
      "description": "URL on service website where the user will be redirected if he cancels the link request operation"
    }
  }
}